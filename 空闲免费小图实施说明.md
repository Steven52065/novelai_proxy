# 空闲免费小图实施说明

本功能在原「免费小图每日数量限制」用尽后，允许用户利用**当前空闲的上游账号**继续生成**确定免费**的小图，并使用独立的每日次数池计数。

## 全局配置

配置文件 `config.yaml` 中的 `idle_free_small`：

- `occupancy_threshold_percent`：默认 `50`。只检查接纳前的占用率，必须**严格小于**该阈值；最后一个请求允许越过阈值。
- `min_idle_seconds`：默认 `30`。请求允许的上游集合必须**连续**低于阈值这么久才会被判定为空闲。

全局参数为只读展示，修改配置文件后重启服务生效。本项目按单进程部署设计，占用率历史只保存在进程内。

## 用户/组倍率

- 用户和用户组都有 `idle_free_small_multiplier`，默认 `0`（关闭）。
- 每日额外次数上限 = `floor(用户自己的原日限额 × 用户自己的倍率)`。
- 例：`100 × 0.29 = 29`，`10 × 0.5 = 5`，`3 × 0.5 = 1`，`1 × 0.5 = 0`。
- 只有原每日免费小图限额已启用、倍率大于 0、计算出的上限至少为 1 时才生效。
- 倍率是独立的可传播成员字段；组默认值、组传播范围与「同步到成员」都支持单独同步该字段。
- 原日限额关闭时允许保存非零倍率，但实际不生效。

## 行为约定

- 只有原每日次数预留抛出 `FreeSmallDailyLimitExceeded` 后才尝试空闲兜底。
- 请求必须同时满足 `action == "generate"`、`estimated_cost == 0`、`cost_is_certainly_free == True`。
- 原次数池与空闲次数池使用同一个准入时间点，避免跨日落入不同窗口。
- 判断范围是请求允许的上游与当前启用上游的交集；空允许列表代表全部启用上游。
- 单上游在空闲时也可以接纳一个请求。
- 入队通过后，真正运行前不再满足空闲条件会释放空闲预留并返回原日限额错误。
- 429 重试会重新检查连续空闲时间；不满足时不会创建下一次尝试日志。
- 服务启动前的时间不计入空闲历史；队列内管理探测等所有进入串行 worker 的动作都计占用。
- 关闭功能（倍率设回 0）后，已预留请求按既定生命周期结束，已用次数保留。

## 升级顺序

1. 部署前备份数据库。
2. 启动新版本，自动增加字段与表。
3. 确认存量用户和组的倍率均为 0。
4. 管理员对目标用户或组设置倍率。
5. 检查两套次数统计、失败释放与费用为零。
6. 如需停止新的兜底申请，将倍率设回 0。
